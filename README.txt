Run main.py to run the chat bot on command line.
Run the server.py deploy your flask server and open the android app to run the chat bot there

Use pip install to install python-aiml before running the program.